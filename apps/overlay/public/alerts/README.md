# Overlay Alert Assets

알림 overlay에서 사용할 GIF, PNG, WebP, MP3, WAV 파일을 이 폴더에 넣고 `apps/server/config/alert-overlays.json`에서 `/alerts/file-name.gif` 또는 `/alerts/file-name.mp3` 형태로 참조합니다.

방송 화면에 노출되는 public asset 폴더이므로 token, secret, 개인 정보가 들어간 파일을 두지 마세요.
